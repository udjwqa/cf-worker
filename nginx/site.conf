limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

upstream fastapi_backend {
    server 127.0.0.1:8000;
    keepalive 32;
}

upstream panel_frontend {
    server 127.0.0.1:3000;
    keepalive 16;
}

server {
    listen 80;
    server_name _;

    # Admin panel
    location / {
        proxy_pass http://panel_frontend;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Scoring Engine
    location /engine/ {
        limit_req zone=api_limit burst=20 nodelay;

        proxy_pass http://fastapi_backend/;
        proxy_http_version 1.1;
        proxy_set_header Connection "";

        set $real_ip $remote_addr;
        if ($http_x_forwarded_for) {
            set $real_ip $http_x_forwarded_for;
        }

        proxy_set_header X-Real-IP $real_ip;
        proxy_set_header X-Forwarded-For $real_ip;
        proxy_set_header X-CF-Country $http_x_cf_country;
        proxy_set_header X-CF-ASN $http_x_cf_asn;
        proxy_set_header X-CF-Ray $http_cf_ray;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass_request_headers on;

        proxy_connect_timeout 5s;
        proxy_read_timeout 30s;
        proxy_send_timeout 5s;
    }
}

# SSL (uncomment after: certbot --nginx -d your-domain.com)
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#     ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#     # ... same location blocks as above ...
# }
